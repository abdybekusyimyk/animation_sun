import 'package:flutter/material.dart';

class Negisgi extends StatelessWidget {
  const Negisgi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
